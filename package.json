{
  "name": "futsal-tactic-board-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix react-prototype",
    "build": "npm run build --prefix react-prototype",
    "lint": "npm run lint --prefix react-prototype",
    "preview": "npm run preview --prefix react-prototype"
  }
}
